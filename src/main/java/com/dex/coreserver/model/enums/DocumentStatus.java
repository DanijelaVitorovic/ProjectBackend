package com.dex.coreserver.model.enums;

public enum DocumentStatus {
    PROCEEDING,
    VERIFICATION,
    SIGNING,
    SIGNED,
    FIRST_SENDING,
    FIRST_RETURNED,
    SECOND_SENDING,
    SECOND_RETURNED,
    BULLETIN_BOARD,
    DELIVERED,
    FINAL,
    REVOKED;
}
